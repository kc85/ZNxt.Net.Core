
@{
	var products = ExecuteAction("/api/ecomm/products",null);
}
   @foreach (var product in @products["data"])
    {
        if(product.images!=null){
          
		    @Include("./template/blocks/index/product.zb",@product)
        }
     }

	@if(@products["data"].Count == 0 ){
		<i> No result found </i>
	}
